<?php
defined('MOODLE_INTERNAL') || die();

$plugin->version   = 2026031204;        // YYYYMMDDXX format, last two digits = incremental
$plugin->requires  = 2022112800;        // Moodle 4.2 minimum version
$plugin->component = 'theme_mytheme';   // Full plugin name
$plugin->release   = 'v1.0.1-filepicker-fix';            // Human-readable version string
